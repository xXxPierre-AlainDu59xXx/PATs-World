/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur
if (instance_exists(Chef))
{
	if (mouse_check_button_pressed(mb_left) && x == this.x && y == this.y)
	{
		
	}
}